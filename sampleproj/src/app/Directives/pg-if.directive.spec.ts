import { PgIfDirective } from './pg-if.directive';

describe('PgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new PgIfDirective();
    expect(directive).toBeTruthy();
  });
});
