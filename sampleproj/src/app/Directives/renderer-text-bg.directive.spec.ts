import { RendererTextBgDirective } from './renderer-text-bg.directive';

describe('RendererTextBgDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererTextBgDirective();
    expect(directive).toBeTruthy();
  });
});
